-- | Tool: write (or overwrite) a file.
module OpenCode.Tool.WriteFile
  ( writeFileTool
  ) where

import Data.Text (Text)
import OpenCode.Tool.Types (SomeTool)

writeFileTool :: SomeTool
writeFileTool = error "OpenCode.Tool.WriteFile.writeFileTool: not yet implemented"

_suppress :: Text
_suppress = "placeholder"
