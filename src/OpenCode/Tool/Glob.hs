-- | Tool: match files against a glob pattern.
module OpenCode.Tool.Glob
  ( globTool
  ) where

import Data.Text (Text)
import OpenCode.Tool.Types (SomeTool)

globTool :: SomeTool
globTool = error "OpenCode.Tool.Glob.globTool: not yet implemented"

_suppress :: Text
_suppress = "placeholder"
